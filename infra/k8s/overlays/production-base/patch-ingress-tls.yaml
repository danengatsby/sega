apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sega-ingress
  namespace: sega
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - sega-contab.online
      secretName: sega-app-tls
